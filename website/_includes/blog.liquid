---
layout: base-new.njk
---

<style>
main h1 {
    font-size: 1em;
}

main {
    h1 { 
        line-height: 1.3;
        margin-top: 2.25em;
        margin-bottom: 1em;

        color: #513c73;
    }

    h2 {
        line-height: 1.3;
        margin-top: 2.2em;
        margin-bottom: 0.75em;

        color: #6d598a;
    }

    h3 { 
        line-height: 1.3;
        margin-top: 1em;
        margin-bottom: 0.35em;

        color: #6d598a;
    }

    h1::before {
        display: inline;
        float: left;
        position: relative;
        width: 3em;
        margin-left: -4em;
        text-align: right;

        content: '•';
    }

    h2::before {
        display: inline;
        float: left;
        position: relative;
        width: 3em;
        margin-left: -4em;
        text-align: right;

        content: '••';
    }

    h3::before {
        display: inline;
        float: left;
        position: relative;
        width: 3em;
        margin-left: -4em;
        text-align: right;

        content: '•••';
    }

    aside, .aside {
        font-size: 0.8em;
        line-height: 1.5;

        /* This is text-keynote-500 */
        color: #8877a1;

        /* This is the 'lg' breakpoint in tailwind */
        @media (min-width: 1024px) {
            display: inline;
            float: left;
            position: relative;
            width: 20vw;
            margin-left: -22vw;
            text-align: right;

        }

        @media (max-width: 1024px) {
            display: block;
            float: none;
            margin: 3% 2.5% 3% 2.5%;
            width: 80%;
        } 
    }
}
</style>

<div class="mt-32">
    <div class="grid gap-2 grid-cols-1 gap-8 lg:gap-4 lg:grid-cols-13">
        <p class="text-keynote-500 font-xanh text-2xl lg:col-start-7 lg:col-span-6">{{ date | date: "%Y-%m-%d" }}</p>
    </div>

    <div class="grid gap-2 grid-cols-1 gap-8 lg:gap-4 lg:grid-cols-13">
        <p class="font-xanh text-2xl lg:col-start-7 lg:col-span-6">{{ title }}</p>
    </div>

    <div class="grid gap-2 grid-cols-1 gap-8 lg:gap-4 lg:grid-cols-13 mt-12">
        <main class="text-gray-700 text-lg lg:col-start-7 lg:col-span-6 max-w-prose">
            {{ content }}
        </main>
    </div>
</div>
